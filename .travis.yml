language: node_js

node_js: "6"

cache:
  yarn: true
  directories:
    - node_modules

before_script:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3

script: $TRAVIS_BUILD_DIR/.travis.sh

sudo: required
