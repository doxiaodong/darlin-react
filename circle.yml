machine:
  node:
    version: 7.9.0
  pre:
    - mkdir ~/.yarn-cache
dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  cache_directories:
    - "~/.yarn-cache"
  override:
    - yarn install
test:
  pre:
    - npm run lint
    - npm run test
    - npm run build:prod
