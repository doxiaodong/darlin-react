machine:
  node:
    version: 7.10.0
dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - yarn install
test:
  override:
    - npm run lint
    - npm run test
    - npm run build:prod
